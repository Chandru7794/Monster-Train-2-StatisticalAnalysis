name: 'monster_train'
version: '1.0.0'

profile: 'mt2'

model-paths: ["models"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  monster_train:
    base:
      +materialized: view
    staging:
      +materialized: view
